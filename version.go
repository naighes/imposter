package main

import (
	"github.com/naighes/imposter/version"
)

var GitCommit string

const Version = version.Version
const ProductName = version.ProductName

var VersionPrerelease = version.Prerelease
